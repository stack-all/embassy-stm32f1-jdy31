[target.thumbv7m-none-eabi] # STM32F1 (Cortex-M3)
runner = "probe-rs run --chip STM32F103C8Tx" # 根据具体 MCU 型号和 probe-rs 支持调整

[build]
target = "thumbv7m-none-eabi"

[env]
DEFMT_LOG = "trace" # 设置 defmt 日志级别